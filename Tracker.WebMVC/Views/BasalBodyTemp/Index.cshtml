@model IEnumerable<Tracker.Models.BBTModels.BBTListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "BBTCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BBTID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Temperature)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfTemp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BBTID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Temperature)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfTemp)
        </td>
        <td>
            @Html.ActionLink("Edit", "BBTEdit", new { id=item.BBTID }) |
            @Html.ActionLink("Details", "BBTDetails", new { id=item.BBTID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BBTID })
        </td>
    </tr>
}

</table>
